# This script contains all the tuning parameters and any input values  
# which might need to be changed in the course of our analysis

train_metric <- "ROC"
train_preProc <- c("center", "scale")
train_tuneLength <- 10
